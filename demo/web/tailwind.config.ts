import type { Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: '#0ea5e9',
        accent: '#22c55e',
      },
    },
  },
  plugins: [],
}
export default config 